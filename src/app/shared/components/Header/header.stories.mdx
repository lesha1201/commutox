import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';

import Header from './index';

# Header

<Meta title="App Components/Header" component={Header} />

## Stories

### Default

<Canvas withToolbar>
  <Story name="default" height="200px" inline={false}>
    <div style={{ margin: '-1rem' }}>
      <Header
        style={{
          display: 'flex',
          alignItems: 'center',
          backgroundColor: 'dodgerblue',
          color: 'white',
          fontWeight: 'bold',
        }}
      >
        Header content
      </Header>
      <Header.Wrapper>
        {[...Array(20).keys()].map(val => (
          <div
            key={val}
            style={{
              padding: '10px 0',
            }}
          >
            Content
          </div>
        ))}
      </Header.Wrapper>
    </div>
  </Story>
</Canvas>

## Props

<ArgsTable of={Header} />
