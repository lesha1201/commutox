---
name: Button
menu: UI Kit
---

import { Playground, Props } from 'docz';
import Margin from 'src/app/shared/docz-helpers/Margin';
import Button from './index';

# Button

`<Button />` is just html tag `<button />` with styles. All dom attrs of a button is acceptable.

## Types

<Playground>
  <div style={{ fontSize: '16px' }}>

    <Button style={{ marginRight: '1em' }}>Default</Button>

    <Button theme="outlined" style={{ marginRight: '1em' }}>
      Outlined
    </Button>

    <Button disabled style={{ marginRight: '1em' }}>
      Disabled
    </Button>

    <Button disabled theme="outlined" style={{ marginRight: '1em' }}>
      Disabled
    </Button>

  </div>
</Playground>

## Sizes

<Playground>
  <div style={{ fontSize: '16px' }}>

    <Margin type="bottom">
      <Button>Default</Button>
    </Margin>

    <Margin type="bottom">
      <Button size="wide">Wide</Button>
    </Margin>

    <Margin>
      <Button size="full">Full</Button>
    </Margin>

  </div>
</Playground>

## Colors

<Playground>
  <div style={{ fontSize: '16px' }}>

    <div>
    <h3>Default</h3>
    <Button mainColor="primary" style={{ marginRight: '1em' }}>Primary</Button>

    <Button mainColor="success" style={{ marginRight: '1em' }}>
      Success
    </Button>

    <Button mainColor="danger">
      Danger
    </Button>
    </div>

    <div>
    <h3>Outlined</h3>
    <Button mainColor="primary" theme="outlined" style={{ marginRight: '1em' }}>Primary</Button>

    <Button mainColor="success" theme="outlined" style={{ marginRight: '1em' }}>
      Success
    </Button>

    <Button mainColor="danger" theme="outlined">
      Danger
    </Button>
    </div>

  </div>
</Playground>

## "As" Prop

You can specify what element to use.

```jsx
// Using tag name
<Button as="a" href="#">Link</Button>

// Using another component (should pass className prop in order styles to be applied)
<Button as={Link} to="#">Router Link</Button>
```

## Props

<Props of={Button} />
