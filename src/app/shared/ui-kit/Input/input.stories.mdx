import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';

import { Margin } from 'app/shared/storybook-helpers';
import Input from './index';

# Input

<Meta title="UI Kit/Input" component={Input} />

## Stories

### Default

<Canvas withToolbar>
  <Story name="default">
    <div style={{ fontSize: '14px' }}>
      <Input placeholder="First name" />
    </div>
  </Story>
</Canvas>

### Width modifiers

<Canvas withToolbar>
  <Story name="width modifiers">
    <div style={{ fontSize: '14px' }}>
      <Margin type="bottom">
        <Input placeholder="Small" width="small" />
      </Margin>
      <Margin type="bottom">
        <Input placeholder="Medium" width="medium" />
      </Margin>
      <Margin type="bottom">
        <Input placeholder="Large" width="large" />
      </Margin>
      <Margin type="top">
        <Input placeholder="Full" width="full" />
      </Margin>
    </div>
  </Story>
</Canvas>

### Before/after addons

<Canvas withToolbar>
  <Story name="before/after addons">
    <div style={{ fontSize: '14px' }}>
      <Margin type="bottom">
        <Input placeholder="Addons" addonBefore="Before" />
      </Margin>
      <Margin type="bottom">
        <Input placeholder="Addons" addonAfter="After" />
      </Margin>
      <Margin type="bottom">
        <Input placeholder="Addons" addonBefore="Before" addonAfter="After" />
      </Margin>
    </div>
  </Story>
</Canvas>

### Disabled

<Canvas withToolbar>
  <Story name="sisabled">
    <div style={{ fontSize: '14px' }}>
      <Input placeholder="Disabled" disabled />
    </div>
  </Story>
</Canvas>

### Error

<Canvas withToolbar>
  <Story name="error">
    <div style={{ fontSize: '14px' }}>
      <Input placeholder="Error" hasError />
    </div>
  </Story>
</Canvas>

## Props

<ArgsTable of={Input} />
